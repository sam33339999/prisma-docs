---
title: 'Prisma Studio'
metaTitle: 'Prisma Studio for Prisma Postgres'
metaDescription: "學習 關於 the various ways of 使用 Prisma Studio, from running locally, to using it in VS Code to 嵌入 it in your own application."
tocDepth: 3
toc: true
---

## 概述

Prisma Postgres comes with Prisma Studio built-in. 您可以 use it in several ways:

- Run `npx prisma studio --url "postgresql://user:password@localhost:5432/dbname"` 使用 Studio locally on your machine (also 適用於 any other database)
- Find the **Studio** tab in your project in the [Prisma Console](https://console.prisma.io) 使用 Studio online
- Install the [Prisma VS Code extension](https://marketplace.visualstudio.com/items?itemName=Prisma.prisma) 使用 Studio directly in VS Code
- [Embed Prisma Studio](/postgres/database/prisma-studio/embedding-studio) in your own app (e.g. as an admin dashboard)

:::note

If you want 使用 Prisma Studio with another database than Prisma Postgres, check the docs [here](/orm/tools/prisma-studio). 

:::