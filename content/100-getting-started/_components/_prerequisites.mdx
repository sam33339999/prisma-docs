您需要：

- 在您的電腦上安裝 [Node.js](https://nodejs.org/en/) v20.19+、v22.12+ 或 v24.0+
- JavaScript 或 TypeScript 基礎知識

{props.database && (
  <ul>
    <li>可存取的 {props.database} 資料庫</li>
    {props.connectionDetails && <li>{props.connectionDetails}</li>}
  </ul>
)}
