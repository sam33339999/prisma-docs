---
title: 'SQL Server 和 MongoDB 中參照動作的特殊規則'
metaTitle: 'SQL Server 和 MongoDB 中參照動作的特殊規則'
metaDescription: '循環引用或多個級聯路徑可能在 Microsoft SQL Server 和 MongoDB 上引起驗證錯誤。由於資料庫不能開箱即用地處理這些情況，請了解如何解決此問題。'
tocDepth: 3
---

:::info 快速摘要
本頁說明在 SQL Server 和 MongoDB 中使用參照動作時的特殊規則和常見問題，包括如何避免循環和多個級聯路徑。
:::


<TopBlock>

某些資料庫在使用參照動作時有特定的要求需要考慮。

- Microsoft SQL Server 不允許外鍵上的級聯參照動作，如果關聯鏈導致循環或多個級聯路徑。如果外鍵上的參照動作設定為 `NO ACTION` 以外的值（或者如果 Prisma ORM 正在管理參照完整性則為 `NoAction`），伺服器將檢查循環或多個級聯路徑，並在執行 SQL 時返回錯誤。

- 對於 MongoDB，在 Prisma ORM 中使用參照動作要求對於具有自我參照關聯或三個 model 之間循環的任何資料模型，您必須將參照動作設定為 `NoAction`，以防止參照動作模擬無限循環。請注意，預設情況下，MongoDB 使用 `relationMode = "prisma"` 模式，這意味著 Prisma ORM 管理[參照完整性](/orm/prisma-schema/data-model/relations/relation-mode)。

給定以下 SQL：

```sql
CREATE TABLE [dbo].[Employee] (
  [id] INT NOT NULL IDENTITY(1,1),
  [managerId] INT,
  CONSTRAINT [PK__Employee__id] PRIMARY KEY ([id])
);

ALTER TABLE [dbo].[Employee]
  ADD CONSTRAINT [FK__Employee__managerId]
    FOREIGN KEY ([managerId]) REFERENCES [dbo].[Employee]([id])
    ON DELETE CASCADE ON UPDATE CASCADE;
```

當執行 SQL 時，資料庫會拋出以下錯誤：

```terminal wrap
Introducing FOREIGN KEY constraint 'FK__Employee__managerId' on table 'Employee' may cause cycles or multiple cascade paths. Specify ON DELETE NO ACTION or ON UPDATE NO ACTION, or modify other FOREIGN KEY constraints.
```

在更複雜的資料模型中，尋找級聯路徑可能會變得複雜。因此在 Prisma ORM 中，資料模型在生成任何要在遷移期間執行的 SQL _之前_進行驗證，突出顯示屬於路徑一部分的關聯。這使得尋找和打破這些動作鏈變得容易得多。

</TopBlock>

<details>
<summary>本頁回答的問題</summary>

- 如何修復 SQL Server 上的級聯循環？
- MongoDB 自我關聯是否需要 NoAction？
- 如何處理多個級聯路徑？

</details>

## 自我關聯（SQL Server 和 MongoDB）

以下 model 描述了一個自我關聯，其中 `Employee` 可以有一個經理和被管理者，引用同一個 model 的條目。

```prisma
model Employee {
  id        Int        @id @default(autoincrement())
  manager   Employee?  @relation(name: "management", fields: [managerId], references: [id])
  managees  Employee[] @relation(name: "management")
  managerId Int?
}
```

這將導致以下錯誤：

```terminal wrap
Error parsing attribute "@relation": A self-relation must have `onDelete` and `onUpdate` referential actions set to `NoAction` in one of the @relation attributes. (Implicit default `onDelete`: `SetNull`, and `onUpdate`: `Cascade`)
```

透過不定義任何動作，Prisma ORM 將根據底層[純量欄位](/orm/prisma-schema/data-model/models#scalar-fields)是設定為選填還是必填來使用以下預設值。

| 子句       | 所有純量欄位都是選填的 | 至少有一個純量欄位是必填的 |
| :--------- | :------------------------------------ | :------------------------------------ |
| `onDelete` | `SetNull`                             | `NoAction`                            |
| `onUpdate` | `Cascade`                             | `Cascade`                             |

由於上述關聯中 `onUpdate` 的預設參照動作是 `Cascade`，而 `onDelete` 是 `SetNull`，這會建立一個循環，解決方案是明確地將 `onUpdate` 和 `onDelete` 值設定為 `NoAction`。

```prisma highlight=3;delete|4;add
model Employee {
  id        Int        @id @default(autoincrement())
  //delete-next-line
  manager   Employee   @relation(name: "management", fields: [managerId], references: [id])
  //add-next-line
  manager   Employee   @relation(name: "management", fields: [managerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  managees  Employee[] @relation(name: "management")
  managerId Int
}
```

## 三個資料表之間的循環關聯（SQL Server 和 MongoDB）

以下 model 描述了 `Chicken`、`Egg` 和 `Fox` 之間的循環關聯，其中每個 model 都引用另一個。

```prisma
model Chicken {
  id        Int   @id @default(autoincrement())
  egg       Egg   @relation(fields: [eggId], references: [id])
  eggId     Int
  predators Fox[]
}

model Egg {
  id         Int       @id @default(autoincrement())
  predator   Fox       @relation(fields: [predatorId], references: [id])
  predatorId Int
  parents    Chicken[]
}

model Fox {
  id        Int     @id @default(autoincrement())
  meal      Chicken @relation(fields: [mealId], references: [id])
  mealId    Int
  foodStore Egg[]
}
```

這將導致循環中屬於一部分的每個關聯欄位產生三個驗證錯誤。

第一個錯誤在 `Chicken` model 中的關聯 `egg`：

```terminal wrap
Error parsing attribute "@relation": Reference causes a cycle. One of the @relation attributes in this cycle must have `onDelete` and `onUpdate` referential actions set to `NoAction`. Cycle path: Chicken.egg → Egg.predator → Fox.meal. (Implicit default `onUpdate`: `Cascade`)
```

第二個錯誤在 `Egg` model 中的關聯 `predator`：

```terminal wrap
Error parsing attribute "@relation": Reference causes a cycle. One of the @relation attributes in this cycle must have `onDelete` and `onUpdate` referential actions set to `NoAction`. Cycle path: Egg.predator → Fox.meal → Chicken.egg. (Implicit default `onUpdate`: `Cascade`)
```

第三個錯誤在 `Fox` model 中的關聯 `meal`：

```terminal wrap
Error parsing attribute "@relation": Reference causes a cycle. One of the @relation attributes in this cycle must have `onDelete` and `onUpdate` referential actions set to `NoAction`. Cycle path: Fox.meal → Chicken.egg → Egg.predator. (Implicit default `onUpdate`: `Cascade`)
```

由於關聯欄位是必填的，`onDelete` 的預設參照動作是 `NoAction`，但 `onUpdate` 是 `Cascade`，這會導致參照動作循環。解決方案是在任何一個關聯中將 `onUpdate` 值設定為 `NoAction`。

```prisma highlight=3;delete|4;add
model Chicken {
  id        Int   @id @default(autoincrement())
  //delete-next-line
  egg       Egg   @relation(fields: [eggId], references: [id])
  //add-next-line
  egg       Egg   @relation(fields: [eggId], references: [id], onUpdate: NoAction)
  eggId     Int
  predators Fox[]
}
```

或

```prisma highlight=3;delete|4;add
model Egg {
  id         Int       @id @default(autoincrement())
  //delete-next-line
  predator   Fox       @relation(fields: [predatorId], references: [id])
  //add-next-line
  predator   Fox       @relation(fields: [predatorId], references: [id], onUpdate: NoAction)
  predatorId Int
  parents    Chicken[]
}
```

或

```prisma highlight=3;delete|4;add
model Fox {
  id        Int     @id @default(autoincrement())
  //delete-next-line
  meal      Chicken @relation(fields: [mealId], references: [id])
  //add-next-line
  meal      Chicken @relation(fields: [mealId], references: [id], onUpdate: NoAction)
  mealId    Int
  foodStore Egg[]
}
```

## 兩個 model 之間的多個級聯路徑（僅限 SQL Server）

資料模型描述了相同 model 之間的兩個不同路徑，兩個關聯都觸發級聯參照動作。

```prisma
model User {
  id       Int       @id @default(autoincrement())
  comments Comment[]
  posts    Post[]
}

model Post {
  id       Int       @id @default(autoincrement())
  authorId Int
  author   User      @relation(fields: [authorId], references: [id])
  comments Comment[]
}

model Comment {
  id          Int  @id @default(autoincrement())
  writtenById Int
  postId      Int
  writtenBy   User @relation(fields: [writtenById], references: [id])
  post        Post @relation(fields: [postId], references: [id])
}
```

此資料模型的問題在於從 `Comment` 到 `User` 有兩條路徑，以及兩個關聯中預設的 `onUpdate` 動作都是 `Cascade`。這導致兩個驗證錯誤：

第一個錯誤在關聯 `writtenBy` 中：

```terminal wrap
Error parsing attribute "@relation": When any of the records in model `User` is updated or deleted, the referential actions on the relations cascade to model `Comment` through multiple paths. Please break one of these paths by setting the `onUpdate` and `onDelete` to `NoAction`. (Implicit default `onUpdate`: `Cascade`)
```

第二個錯誤在關聯 `post` 中：

```terminal wrap
Error parsing attribute "@relation": When any of the records in model `User` is updated or deleted, the referential actions on the relations cascade to model `Comment` through multiple paths. Please break one of these paths by setting the `onUpdate` and `onDelete` to `NoAction`. (Implicit default `onUpdate`: `Cascade`)
```

該錯誤意味著透過更新 `User` model 中記錄的主鍵，更新將透過 `writtenBy` 關聯在 `Comment` 和 `User` 之間級聯一次，並且由於 `Post` 與 `Comment` model 相關，將再次透過 `Post` model 從 `post` 關聯級聯。

修復方法是在 `writtenBy` 或 `post` 關聯欄位中將 `onUpdate` 參照動作設定為 `NoAction`，或者從 `Post` model 透過更改 `author` 關聯中的動作：

```prisma highlight=5;delete|6;add
model Comment {
  id          Int  @id @default(autoincrement())
  writtenById Int
  postId      Int
  //delete-next-line
  writtenBy   User @relation(fields: [writtenById], references: [id])
  //add-next-line
  writtenBy   User @relation(fields: [writtenById], references: [id], onUpdate: NoAction)
  post        Post @relation(fields: [postId], references: [id])
}
```

或

```prisma highlight=6;delete|7;add
model Comment {
  id          Int  @id @default(autoincrement())
  writtenById Int
  postId      Int
  writtenBy   User @relation(fields: [writtenById], references: [id])
  //delete-next-line
  post        Post @relation(fields: [postId], references: [id])
  //add-next-line
  post        Post @relation(fields: [postId], references: [id], onUpdate: NoAction)
}
```

或

```prisma highlight=4;delete|5;add
model Post {
  id       Int       @id @default(autoincrement())
  authorId Int
  //delete-next-line
  author   User      @relation(fields: [authorId], references: [id])
  //add-next-line
  author   User      @relation(fields: [authorId], references: [id], onUpdate: NoAction)
  comments Comment[]
}
```
