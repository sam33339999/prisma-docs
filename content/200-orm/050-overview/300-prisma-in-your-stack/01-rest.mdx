---
title: 'REST'
metaTitle: '使用 Prisma ORM 建立 REST API'
metaDescription: '本頁面概述了使用 Prisma 建立 REST API 時最重要的事項。它展示了實用範例和支援的函式庫。'
---

<TopBlock>

在建立 REST API 時,Prisma Client 可以在你的_路由控制器_內部使用來發送資料庫查詢。

![REST APIs with Prisma Client](../100-introduction/prisma-rest-apis.png)

</TopBlock>

## 支援的函式庫

由於 Prisma Client「只」負責向你的資料庫發送查詢,它可以與你選擇的任何 HTTP 伺服器函式庫或網頁框架結合使用。

以下是可與 Prisma ORM 一起使用的函式庫和框架的非詳盡清單:

- [Express](https://expressjs.com/)
- [koa](https://koajs.com/)
- [hapi](https://hapi.dev/)
- [Fastify](https://fastify.dev/)
- [Sails](https://sailsjs.com/)
- [AdonisJs](https://adonisjs.com/)
- [NestJS](https://nestjs.com/)
- [Next.js](https://nextjs.org/)
- [Foal TS](https://foalts.org/)
- [Polka](https://github.com/lukeed/polka)
- [Micro](https://github.com/zeit/micro)
- [Feathers](https://feathersjs.com/)
- [Remix](https://remix.run/)

## REST API 伺服器範例

假設你有一個類似於以下的 Prisma schema:

```prisma
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client"
  output   = "./generated"
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    User?   @relation(fields: [authorId], references: [id])
  authorId  Int?
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
  posts Post[]
}
```

你現在可以實作路由控制器(例如使用 Express),在收到傳入的 HTTP 請求時使用生成的 [Prisma Client API](/orm/prisma-client) 來執行資料庫操作。本頁面只展示了幾個範例程式碼片段;如果你想執行這些程式碼片段,可以使用 [REST API 範例](https://pris.ly/e/ts/rest-express)。

#### `GET`

```ts
app.get('/feed', async (req, res) => {
  const posts = await prisma.post.findMany({
    where: { published: true },
    include: { author: true },
  })
  res.json(posts)
})
```

請注意,在此情況下,`feed` 端點返回一個巢狀的 JSON 回應,其中的 `Post` 物件_包含_一個 `author` 物件。以下是範例回應:

```json
[
  {
    "id": "21",
    "title": "Hello World",
    "content": "null",
    "published": "true",
    "authorId": 42,
    "author": {
      "id": "42",
      "name": "Alice",
      "email": "alice@prisma.io"
    }
  }
]
```

#### `POST`

```ts
app.post(`/post`, async (req, res) => {
  const { title, content, authorEmail } = req.body
  const result = await prisma.post.create({
    data: {
      title,
      content,
      published: false,
      author: { connect: { email: authorEmail } },
    },
  })
  res.json(result)
})
```

#### `PUT`

```ts
app.put('/publish/:id', async (req, res) => {
  const { id } = req.params
  const post = await prisma.post.update({
    where: { id: Number(id) },
    data: { published: true },
  })
  res.json(post)
})
```

#### `DELETE`

```ts
app.delete(`/post/:id`, async (req, res) => {
  const { id } = req.params
  const post = await prisma.post.delete({
    where: {
      id: Number(id),
    },
  })
  res.json(post)
})
```

## 立即可執行的範例專案

你可以在 [`prisma-examples`](https://github.com/prisma/prisma-examples/) 儲存庫中找到多個立即可執行的範例,展示如何使用 Prisma Client 實作 REST API,以及建立完整的應用程式。

| **範例**                                                                                                                   | **技術堆疊**    | **說明**                                |
| ----------------------------------------------------------------------------------------------------------------------------- | ------------ | ---------------------------------------------- |
| [`express`](https://pris.ly/e/ts/rest-express)                               | 僅後端 | 使用 Express 的 TypeScript REST API           |
| [`fastify`](https://pris.ly/e/ts/rest-fastify)                               | 僅後端 | 使用 Fastify 和 Prisma Client 的 REST API      |
| [`hapi`](https://pris.ly/e/ts/rest-hapi)                                     | 僅後端 | 使用 hapi 和 Prisma Client 的 REST API          |
| [`nestjs`](https://pris.ly/e/ts/rest-nestjs)                                 | 僅後端 | 具有 REST API 的 Nest.js 應用程式 (Express)          |
| [`nextjs`](https://pris.ly/e/orm/nextjs)                                     | 全端    | 具有 REST API 的 Next.js 應用程式 (React)            |
